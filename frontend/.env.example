# Copy to `.env.local` for local development (`npm run dev`).
# Default in code matches backend launch profile (http://localhost:5252).
NEXT_PUBLIC_API_BASE_URL=http://localhost:5252
# Set to 1 to show the maintenance page to visitors.
MAINTENANCE_MODE=0
